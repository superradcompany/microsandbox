module github.com/superradcompany/microsandbox/sdk/go

go 1.22

// v0.6.5 shipped breaking wire changes as a patch release and was withdrawn
// across registries; the module proxy is immutable, so retract it here.
retract v0.6.5
