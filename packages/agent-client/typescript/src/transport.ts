/** Exclusive ordered byte transport; framing and correlation belong to the shared engine. */
export type { ByteTransport as AgentTransport, ByteTransport, Connector, ConnectContext } from "@microsandbox/protocol-client";
