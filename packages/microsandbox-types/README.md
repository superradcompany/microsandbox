# microsandbox-types

Shared microsandbox task-contract types.

```text
microsandbox-types/
|-- rust/        # Rust crate: microsandbox-types
`-- typescript/  # TypeScript package: @microsandbox/types
```

The Rust crate is the source of truth. The TypeScript package is generated from the Rust types with `ts-rs`.
